@model IEnumerable<book_collection.Model.Borrow>

@{
    ViewData["Title"] = "Låna en bok";
}

<h1>Låna en bok</h1>

<p>
    <a class="btn" asp-action="Create">Nytt lån</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BorrowedDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Book)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BorrowedDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Book.Title)
            </td>
            <td class="link-container">
                <a asp-action="Details" asp-route-id="@item.Id"><i class="fa-solid fa-info"></i> Info</a> 
                <a asp-action="Delete" asp-route-id="@item.Id"><i class="fa-solid fa-trash-can"></i> Ta bort lån</a>
            </td>
        </tr>
}
    </tbody>
</table>
