@model IEnumerable<book_collection.Model.Category>

@{
    ViewData["Title"] = "Kategorier";
}

<h1>Kategorier</h1>

<p>
    <a asp-action="Create">Skapa ny kategori</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CategoryName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CategoryName)
            </td>
            <td class="link-container">
                <a asp-action="Edit" asp-route-id="@item.Id"><i class="fa-solid fa-pen"></i> Ändra</a>
                <a asp-action="Delete" asp-route-id="@item.Id"><i class="fa-solid fa-trash-can"></i> Radera</a>
            </td>
        </tr>
    }
    </tbody>
</table>
